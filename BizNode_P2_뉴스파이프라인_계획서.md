# BizNode P2 — 뉴스 파이프라인 계획서

> 근거: `BizNode_KG_추출적재_방법서.md` §8·§12 · P1 완결 상태 위에 구축
> 확정: 2026-07-27

---

## 0. P2의 목표 — P1이 못 채운 것

P1(DART 정형)으로 8엣지를 채웠으나, **나머지 4엣지 + 노드 2종은 정형 소스가 없다.**

| 대상 | P1 현재 | P2 목표 | 왜 정형으로 안 되나 |
|---|---|---|---|
| `COMPETES_WITH` | 0 | 뉴스 실명 근거 | 공시에 경쟁사 실명 없음(법적 리스크로 "경쟁사"라고만 표기) |
| `REGULATES` | 0 | 공정위·금융위 → 기업 | 규제 주체가 공시자가 아님 |
| `IMPACTS` | 0 | Event → 기업 (sign) | 파급 판단은 해석 영역 |
| `SUES` | 0 | 실명 소송 당사자 | 공시 소송은 상대가 익명 개인 |
| `Event` 노드 | 1 | 리콜·규제·사건 다수 | 시드가 건강해 부도·회생 0건 |
| `Organization` 노드 | 0 | 규제기관·정부 | 정형 소스 없음 |

**부수 효과:** P1 미결도 함께 해결 — 이재용·홍라희 ER 병합(기업집단·뉴스 문맥), person 대리키 전환.

---

## 1. 뉴스 본문 확보 — 조사 결과와 결정

### 1-1. 조사 실측 (2026-07-27)

| 소스 | 본문 | 커버리지 | 비용 | 판정 |
|---|---|---|---|---|
| **빅카인즈 API** | **200자 제한**(공식 명세 V1.5) | 106개사(한경·매경·전자신문 포함) | 무료(신청·승인) | ✅ 채택 — 검색·메타 |
| **RSS 전문 6개사** | **전문**(뉴시스 896자·조선비즈 593자 등 중앙값) | 6개사 | 무료·즉시 | ✅ 채택 — 본문 |
| RSS 절단형 | 102~676자 | 한경(태그없음)·매경·전자신문·동아 등 | 무료 | ⛔ 본문 불가 |
| **네이버 검색 API** | 요약만(description) | 넓음 | 무료 | △ 원문 URL 발견용만 |
| **네이버 본문 크롤링** | — | — | — | ⛔ **금지**(아래) |
| 뉴스토어(언론재단) | **전문 API** | 97개사 | **유료**(학술연구용 구분 有) | 🔵 논문·본선 단계 검토 |
| NewsAPI.org / GDELT | 200자 절단 / 메타만 | 해외 중심 | — | ⛔ 부적합 |

### 1-2. ⛔ 네이버 본문 크롤링 배제 (확정)

`n.news.naver.com/robots.txt`가 **우리 용도를 명시적으로 금지**한다:
```
# BOT ACCESS FOR THE PURPOSES OF AI TRAINING AND
# RETRIEVAL-AUGMENTED GENERATION (RAG) IS STRICTLY PROHIBITED.
User-agent: *  Disallow: /
```
한국은 **TDM(텍스트·데이터 마이닝) 면책 조항이 없어** 미국식 fair use 방어가 통하지 않는다.
공모전 산출물이라 데이터 출처를 밝혀야 하므로 리스크가 실질적이다.

### 1-3. 채택: 3단 하이브리드

```
1단 검색·메타 : 빅카인즈 API (106개사) + RSS + 네이버 검색API(원문 URL 발견)
2단 1차 필터  : 리드문(200자)·요약으로 관계 유무 판정 → 크롤링 양 감축
3단 본문 확보 : ① RSS 전문(크롤링 불필요)
               ② 언론사 원문 크롤링 — robots.txt 준수, 네이버 제외
               ③ 실패 시 리드문 폴백
```

**핵심 차이 — 네이버 vs 언론사 원문 크롤링:**

| | 네이버 크롤링 | 언론사 원문 크롤링 |
|---|---|---|
| robots.txt | **AI/RAG 명시 금지** | 매체별 상이, 대개 미금지 |
| 대상 | 재배포 페이지 | **원저작자 사이트** |

→ 전자는 배제, 후자는 **robots.txt 준수 조건부 허용**.

### 1-4. 크롤링 준수 사항 (필수)

- 매체별 `robots.txt` **사전 확인** → 허용된 도메인만 화이트리스트
- 요청 간 **1~2초 딜레이**, User-Agent 명시(연락처 포함)
- **본문 저장 금지** — 추출 후 폐기, `url`만 보관 (방법서 §8)
- 산출물 = **관계 트리플 + 출처 URL** (원문 재배포 아님)
- 차단·실패 시 즉시 중단하고 리드문 폴백

---

## 2. 아키텍처 (방법서 §12)

```
[수집] 시드 64개사 × 관계 키워드
   ↓  빅카인즈 / RSS / 네이버검색
[dedup] 제목 유사도 (통신사 전재 중복 제거)
   ↓
[게이트1] 규칙 필터 — 키워드 無 or 시드 미언급 → 즉시 컷 (무료)
   ↓
[게이트2] 제로샷 LLM 라우터 — gpt-4o-mini, {"is_relevant": bool}
   ↓  상위 20~30%만 통과
[본문] RSS 전문 / 원문 크롤링 / 리드문 폴백
   ↓
[추출] 개체(5종) + 관계(12종 + 매트릭스) — 상위 모델
   ↓
[ER] corp_code 정확매칭 → pg_trgm 블로킹 → Jaro-Winkler → Union-Find
   ↓
[관계정규화] OTHER → NLI 분류(subtype soft-constraint) / 실패 hard-reject
   ↓
[적재] staged_edges → ChromaDB evidence → Neo4j  (P1 §5-2 흐름 재사용)
```

### 2-1. P1 자산 재사용 (신규 개발 최소화)

| 재사용 (그대로) | 신규 개발 |
|---|---|
| `staged_edges` 스테이징 | 뉴스 수집기(빅카인즈·RSS·크롤러) |
| `evidence.py` (ChromaDB) | 적합성 필터 2단 게이트 |
| `graph_loader.py` (Neo4j MERGE) | 개체·관계 추출기(LLM) |
| **`matrix.py` Validator** ★ | ER 강화(블로킹·Jaro-Winkler·Union-Find) |
| `resolver.py` (corp_code) | 관계 정규화(NLI) |
| `audit_graph.py` | |
| `build_all.py` | |

> **매트릭스 Validator가 P2에서 진가를 발휘한다.** P1은 규칙 추출이라 위반 0건이었지만,
> LLM 추출은 `Product -SUPPLIES_TO-> Person` 같은 오류를 낸다. 적재 전 차단된다.

---

## 3. 세부 단계

### P2A. 수집 인프라
- [ ] PostgreSQL `news_articles` 테이블 (url PK·title·press·published_at·lead·collected_at)
- [ ] RSS 수집기 6개사 (뉴시스·조선비즈·서울신문·데일리안·노컷·ZDNet)
- [ ] 빅카인즈 클라이언트 (키 발급 후)
- [ ] 제목 유사도 dedup (통신사 전재 대응)
- [ ] robots.txt 검사기 + 크롤러(딜레이·UA·화이트리스트)

### P2B. 비용 방어 게이트 ★비용의 관건
- [ ] 1차 규칙 필터: 관계 키워드(인수·합병·공급·납품·소송·제휴·지분·규제·경쟁) AND 시드 언급
- [ ] 2차 제로샷 라우터: gpt-4o-mini, `{"is_relevant": bool}` JSON only
- [ ] 통과율·비용 로깅 (목표: 상위 20~30%만 통과)

### P2C. 개체·관계 추출
- [ ] 개체 추출: 5종 노드 타입 제시, 개방 추출
- [ ] 관계 추출: **12종 + 매트릭스 프롬프트 주입**, source=주어/target=목적어 고정
- [ ] IMPACTS `sign`(positive/negative/neutral) 필수 판정
- [ ] 미해당은 `OTHER` + 원문 표현 보존
- [ ] evidence 스니펫 생성(근거 문장 + 앞뒤 문맥)

### P2D. ER 강화 (P1 ER 확장)
- [ ] pg_trgm 블로킹 (`corp_code_master` GIN 인덱스 활용 — Sprint 0에서 준비됨)
- [ ] Jaro-Winkler 정밀 비교
- [ ] Union-Find 클러스터링 → 대표 개체명
- [ ] **person 대리키 전환** (`person_id`) — P1에서 P2로 미룬 항목
- [ ] 기업집단 신호로 이재용·홍라희 병합

### P2E. 샘플 검증 ★먼저 실측
- [ ] 뉴스 200~300건 수집 → 파이프라인 전 구간 통과
- [ ] **실측 지표**: ER 매칭률 / 관계추출 정확도 / OTHER 비율 / 건당 비용 / 매트릭스 위반율
- [ ] 결과 보고 전수 적재 여부 결정

---

## 4. 리스크와 대응

| 리스크 | 대응 |
|---|---|
| 빅카인즈 승인 지연 | **RSS로 선행 개발**, 키 나오면 소스만 추가(수집기 인터페이스 통일) |
| LLM 비용 폭증 | 2단 게이트 + 샘플 검증 먼저. 통과율 실측 후 스케일 결정 |
| LLM 관계 오추출 | 매트릭스 Validator(적재 전 차단) + `confidence` 기록 + evidence 필수 |
| 크롤링 차단 | robots.txt 사전 검사 + 리드문 폴백 + 즉시 중단 |
| 저작권 | 본문 미저장·URL만 보관·트리플만 산출 |
| ER 오병합 | P1 원칙 유지 — 근거 확실한 것만. `source_type="news"`로 DART와 구분 |

---

## 5. 성공 기준

```
✅ 뉴스 200~300건에서 COMPETES_WITH·REGULATES·IMPACTS·SUES 엣지가 실제로 생성
✅ 모든 뉴스 엣지가 evidence(원문 스니펫 + URL)를 가짐 → 팩트체크 가능
✅ confidence·source_type="news"로 DART 관측(1.0)과 명확히 구분
✅ 매트릭스 위반 0건 (Validator 작동)
✅ audit_graph 무결성 통과
```
