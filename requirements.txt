fastapi>=0.115,<1.0
pydantic>=2.10,<3.0
python-dotenv>=1.0,<2.0
uvicorn[standard]>=0.34,<1.0

# HTTP — DART API 호출 · 뉴스 본문 수집
requests>=2.32,<3.0

# XML — 사업보고서 원문 파싱(malformed 복구, recover 파서)
lxml>=5.0,<7.0

# LLM — 경로 C 서술 추출 · evidence 임베딩(text-embedding-3-small)
openai>=1.50,<2.0

# 형태소 분석 — 검색 질의에서 조사를 문법적으로 분리(AnchorExtractor).
# 문자열 휴리스틱(어절 끝 조사 목록 매칭)이 「일이」 같은 조사 잔여물을
# 실존 법인명으로 오인하던 것을 막는다(현황서 §4-1).
# manylinux2014_aarch64 휠이 있어 ARM(t4g) 서버에서 컴파일 없이 설치된다.
# kiwipiepy_model(설치 후 105MB)이 함께 딸려 온다.
kiwipiepy>=0.21,<1.0

# ── 저장소 계층 ────────────────────────────────────────────────
# Graph DB (APOC 프로시저 사용)
neo4j>=5.28,<7.0
# RDBMS — 기업마스터·재무·프로파일·원문메타 + pg_trgm ER 블로킹
psycopg[binary]>=3.2,<4.0
# Vector DB — evidence·프로파일 선별 임베딩 (확장 시 Qdrant로 교체)
# 컨테이너 서버(0.5.23)와 클라이언트 API 버전 정렬 (v2/v1 불일치 방지)
chromadb>=0.5.20,<0.6
# 캐시/큐 (선택) — Tier3 수집 큐, DART rate limit 카운터
redis>=5.0,<7.0

# ── 테스트 ─────────────────────────────────────────────────────
pytest==9.1.1
